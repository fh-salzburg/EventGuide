<%= render 'menu', { :permission => @permission } %>
<div id="left_column">
	<%= render 'eventlist' %>
</div>

<div id="right_column">	
<h1>Event erstellen</h1>

<%= form_for @event do |e| %>
  <% if @event.errors.any? %>
    <div class="error_messages">
      <h3>Es sind folgende Fehler aufgetreten:</h3>
      <ul>
        <% for message in @user.errors.full_messages %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <p>
    <%= e.label :isAStation %>
    <%= radio_button :event, :isAStation, true, :id => "radioButtonStation" %> Station oder Quiz
	<%= radio_button :event, :isAStation, false, :id => "radioButtonEvent" %> Event
  </p>
    <p>
    <%= e.label :name %>
    <%= e.text_field :name %>
  </p>
  <p>
    <%= e.label :room %>
    <%= e.text_field :room, :size => "12" %>
  </p>
  <p>
    <%= e.label :short_description %>
    <%= e.text_area :short_description, :size => "50x1" %>  	
  </p>
   <p>
    <%= e.label :description %>
    <%= e.text_area :description, :size => "50x5" %>  	
  </p> 
   <p>
    <%= e.label :image %>
    <%= e.file_field :image %>  	
  </p> 
  <p class="select_date" >
    <%= e.label :time_from %>
    <%= e.datetime_select :time_from%>  	
  </p>
  <p class="select_date">
    <%= e.label :time_to %>
    <%= e.datetime_select :time_to %>  	
  </p>
  <p class="button"><%= e.submit %></p>
<% end %>
</div>