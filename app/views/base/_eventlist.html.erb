<% @events.each do |event| %>
<div class="eventlist">
  <%= link_to event.title, event, :class => 'event_title' %>
  <%= link_to event.room, event, :class => 'event_room' %>
  <%= link_to event.short_description, event, :class => 'short_description' %>
</div>
<% end %>
