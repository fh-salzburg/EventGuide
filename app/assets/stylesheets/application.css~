/*
 * This is a manifest file that'll automatically include all the stylesheets available in this directory
 * and any sub-directories. You're free to add application-wide styles to this file and they'll appear at
 * the top of the compiled file, but it's generally better to create a new file per style scope.
 *= require_self
 *= require_tree . 
*/

body{
overflow: hidden;
}

h3.login {
	border-bottom: 1px dotted black;
	padding: 5px;
	margin: 0;
}

#event_menu{
	position: absolute;
	top: 0;
	left: 0;
	width: 307px;
	height: 40px;
	background: url('../images/events_menu.png') no-repeat;
	background-position: center 0%; /* 0% um die obere Hälfte des Bildes anzuzeigen */
	background-size: auto 200%; /* 200% streckt das Bild auf doppelte höhe um nur die untere oder die obere Hälfte des Bildes anzeigen zu können*/
	z-index: 40;
}

#event_menu_left{
	position: absolute;
	top: 0;
	left: 0;
	width: 200px;
	height: 40px;
	padding: 8px;
	z-index: 40;
	font-size: 20px;
}

#event_menu_right{
	position: absolute;
	top: 0;
	left: 200px;
	width: 107px;
	height: 4px;
	padding: 8px;
	z-index: 40;
	font-size: 20px;
}

#menu {
	position: absolute;
	border-bottom: 1px solid #e8eae9;
	top: 0;
	left: 307px;
	right: 0;
	height: 24px;
	padding: 8px;

	background: url('../images/oberer_grauer_balken.png') no-repeat;
	background-repeat: repeat-x;
	color: white;
	text-align: right;
	z-index: 30;
	font-size: 20px;
}

#menu a, #event_menu_left a, #event_menu_right a {
	color: #dedede;
	text-decoration: none;
}

#menu a:hover, #event_menu_left a:hover, #event_menu_right a:hover {
	color: white;
}

#left_column {
	position: absolute;
	top: 40px;
	left: 0;
	width: 307px;
	height: 94.3%;
	background-image: -moz-linear-gradient(top, #0e91d5, #1672a1);
	background-image: -webkit-gradient(linear, left top, left bottom, color-stop(0.00, #0e91d5), color-stop(1.0,#1672a1));
	z-index: 20;
}

#eventlist{
	position: relative;
	border-bottom: 1px solid #095e87;
	border-top: 1px solid #68bce8;
	left: 0;
	top: -1px;
	width: 302px;
	height: 97px;
	background-image: -moz-linear-gradient(top, #0e91d5, #0d85c2);
	background-image: -webkit-gradient(linear, left top, left bottom, color-stop(0.00, #0e91d5), color-stop(1.0,#0d85c2));
}

#right_column {
	position: absolute;
	top: 40px;
	left: 307px;
	right: 0;
	bottom: 0;
	background-color: #dedede;
	padding: 3px;
	z-index: 10;
}

#image{
	position: absolute;
	top: 0;
	left: 0;
	right: 0;
	height: 291px;
	background: url('../images/testbild.png') no-repeat;
}

#description{
	position: absolute;
	top: 291px;
	left: 0;
	right: 0;
	bottom: 0;
	background-image: -moz-linear-gradient(top, #f9f9f9, #dcdcdc);
	background-image: -webkit-gradient(linear, left top, left bottom, color-stop(0.00, #f9f9f9), color-stop(1.0,#dcdcdc));
}

#login {
	width: 250px;
}

#login label {
	display: block;
 	width: 80px;
 	float: left;
}

#login .button {
	text-align: center;
}
